apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: master
  namespace: aap-argo
spec:
  syncPolicy:
    preserveResourcesOnDeletion: false
  generators:
  - clusters: {}
  template:
    metadata:
      name: master
    spec:
      project: default
      source:
        repoURL: '{{ gitRepo }}'
        targetRevision: HEAD
        path: appsets
        directory:
          include: '*.yaml'
      destination:
        namespace: '{{ argoNamespace }}'
